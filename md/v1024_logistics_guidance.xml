<?xml version="1.0" encoding="utf-8"?>
<mdscript name="V1024_LogisticsGuidance" description="Logistics guidance and trader efficiency tools">
    <cues>
        <cue name="LG_SettingUp" instantiate="true">
            <conditions>
                <event_cue_signalled cue="md.Setup.Start" />
            </conditions>
            <actions>
                <!-- Stations all have a persistent IDCode ($station.idcode), so we can use that to lookup their "additional settings". -->
                <!-- This IDCode is highly unlikely to be reused, so we should be good. -->
                <!-- String idcode -> number -->
                <show_help position="1" duration="6s" custom="'V1024 Logistics Guidance: starting up.'" chance="0"/>
                <do_if value="not (global.$v1024_logGuideTradeRangeTable?)">
                    <set_value name="global.$v1024_logGuideTradeRangeTable" exact="table[]" />
                </do_if>
                <do_if value="not (global.$v1024_logGuideMineRangeTable?)">
                    <set_value name="global.$v1024_logGuideMineRangeTable" exact="table[]" />
                </do_if>
                <do_if value="not (global.$v1024_logGuideSalvageRangeTable?)">
                    <set_value name="global.$v1024_logGuideSalvageRangeTable" exact="table[]" />
                </do_if>
            </actions>
        </cue>
        <cue name="OnLuaInited" instantiate="true">
            <conditions>
                <event_ui_triggered screen="'Interact_Menu_API'" control="'reloaded'" />
            </conditions>
            <actions>
                <!-- Set up a custom actions group for improved organization -->
                <show_help position="1" duration="6s" custom="'V1024 Logistics Guidance: setting custom actions group.'" chance="0"/>
                <raise_lua_event name="'Interact_Menu_API.Add_Custom_Actions_Group_Id'" param="'trade_range_group'"/>
                <raise_lua_event name="'Interact_Menu_API.Add_Custom_Actions_Group_Text'" param="{251024, 101}"/>
            </actions>
        </cue>
        <cue name="LG_AddInteractActions" instantiate="true">
            <conditions>
                <event_cue_signalled cue="md.Interact_Menu_API.Get_Actions" />
            </conditions>
            <actions>
                <set_value name="$target" exact="event.param.$object"/>
                <do_if value="$target.isplayerowned and $target.isclass.station">
                    <!-- Check whether we have settings for this station -->
                    <set_value name="$idcode" exact="event.param.$object.idcode" />

                    <!-- We split by 3 categories to allow for flexible configuration -->
                    <!-- All categories will have: 
                      - Limit to sector
                      - Limit to cluster (available if cluster has multiple sectors)
                      - Reset to vanilla 
                    -->
                    <set_value name="$menuSection" exact="'trade_range_group'"/>
                    <!-- Check whether the cluster has only 1 sector; hide some redundant options if so. -->
                    <find_sector name="$tempSectors" space="$target.cluster" multiple="true"/>
                    <set_value name="$hasMultipleSectors" exact="$tempSectors.count gt 1" />

                    <!-- Trading range -->
                    <set_value name="$allowTradeSectorRange" exact="@global.$v1024_logGuideTradeRangeTable.$idcode != 'sector'" />
                    <set_value name="$allowTradeClusterRange" exact="@global.$v1024_logGuideTradeRangeTable.$idcode != 'cluster' and $hasMultipleSectors" />
                    <set_value name="$allowTradeDefaultRange" exact="global.$v1024_logGuideTradeRangeTable.$idcode?" />
                    <signal_cue_instantly
                        cue="md.Interact_Menu_API.Add_Action"
                        param = "table[
                        $id         = 'logguide_traderange_sector',
                        $section    = $menuSection,
                        $text       = {251024, 2001},
                        $mouseover  = {251024, 2002}.[$target.name, $target.sector.knownname],
                        $callback   = LogGuide_TradeRange_Sector,
                        $active     = $allowTradeSectorRange,
                        ]"/>
                    <signal_cue_instantly
                        cue="md.Interact_Menu_API.Add_Action"
                        param = "table[
                        $id         = 'logguide_traderange_cluster',
                        $section    = $menuSection,
                        $text       = {251024, 2011},
                        $mouseover  = {251024, 2012}.[$target.name, $target.cluster.knownname],
                        $callback   = LogGuide_TradeRange_Cluster,
                        $active     = $allowTradeClusterRange,
                        ]"
                        chance="if $hasMultipleSectors then 100 else 0"
                        comment="Hide button if sector == cluster to reduce general confusion" />
                    <signal_cue_instantly
                        cue="md.Interact_Menu_API.Add_Action"
                        param = "table[
                        $id         = 'logguide_traderange_reset',
                        $section    = $menuSection,
                        $text       = {251024, 2021},
                        $mouseover  = {251024, 2022}.[$target.name],
                        $callback   = LogGuide_TradeRange_Reset,
                        $active     = $allowTradeDefaultRange,
                        ]"/>
                    
                    <!-- Mining range -->
                    <set_value name="$allowMineSectorRange" exact="@global.$v1024_logGuideMineRangeTable.$idcode != 'sector'" />
                    <set_value name="$allowMineClusterRange" exact="@global.$v1024_logGuideMineRangeTable.$idcode != 'cluster' and $hasMultipleSectors" />
                    <set_value name="$allowMineDefaultRange" exact="global.$v1024_logGuideMineRangeTable.$idcode?" />
                    <signal_cue_instantly
                        cue="md.Interact_Menu_API.Add_Action"
                        param = "table[
                        $id         = 'logguide_minerange_sector',
                        $section    = $menuSection,
                        $text       = {251024, 2101},
                        $mouseover  = {251024, 2102}.[$target.name, $target.sector.knownname],
                        $callback   = LogGuide_TradeRange_Sector,
                        $active     = $allowTradeSectorRange,
                        ]"/>
                    <signal_cue_instantly
                        cue="md.Interact_Menu_API.Add_Action"
                        param = "table[
                        $id         = 'logguide_minerange_cluster',
                        $section    = $menuSection,
                        $text       = {251024, 2111},
                        $mouseover  = {251024, 2112}.[$target.name, $target.cluster.knownname],
                        $callback   = LogGuide_TradeRange_Cluster,
                        $active     = $allowTradeClusterRange,
                        ]"
                        chance="if $hasMultipleSectors then 100 else 0"
                        comment="Hide button if sector == cluster to reduce general confusion" />
                    <signal_cue_instantly
                        cue="md.Interact_Menu_API.Add_Action"
                        param = "table[
                        $id         = 'logguide_minerange_reset',
                        $section    = $menuSection,
                        $text       = {251024, 2121},
                        $mouseover  = {251024, 2122}.[$target.name],
                        $callback   = LogGuide_TradeRange_Reset,
                        $active     = $allowTradeDefaultRange,
                        ]"/>

                    <!-- Salvaging range -->
                    <do_if value="global.$v1024_logGuideSalvageRangeTable.$idcode?">
                        <!-- Has entry; make it reset to vanilla -->
                        <signal_cue_instantly
                            cue="md.Interact_Menu_API.Add_Action"
                            param = "table[
                            $id         = 'logguide_salvagerange999',
                            $section    = $menuSection,
                            $text       = {251024, 2211},
                            $mouseover  = {251024, 2212}.[$target.name],
                            $callback   = LogGuide_SalvageRange999,
                            ]"/>
                    </do_if>
                    <do_else>
                        <!-- No entry; allow limiting to 0 -->
                        <signal_cue_instantly
                            cue="md.Interact_Menu_API.Add_Action"
                            param = "table[
                            $id         = 'logguide_salvagerange0',
                            $section    = $menuSection,
                            $text       = {251024, 2201},
                            $mouseover  = {251024, 2202}.[$target.name, $target.cluster.knownname],
                            $callback   = LogGuide_SalvageRange0,
                            ]"/>
                    </do_else>
                </do_if>
            </actions>
        </cue>

        <!-- Event handlers -->

        <!-- Trade range -->
        <cue name="LogGuide_TradeRange_Sector" instantiate="true" namespace="this">
            <conditions>
                <event_cue_signalled/>
            </conditions>
            <actions>
                <set_value name="$idcode" exact="event.param.$object.idcode" />
                <set_value name="global.$v1024_logGuideTradeRangeTable.$idcode" exact="'sector'" />
                <show_help position="1" duration="6s" custom="'V1024 Logistics Guidance: Trade range becomes sector.'" chance="0"/>
                <speak actor="player.computer" priority="-30">
                    <!--
                        In case for Betty, you can tell her to speak other supported lines by specifying the page number.
                    -->
                    <text line="134" comment="New command set." />
                    <text line="145" comment="Control established." />
                </speak>
            </actions>
        </cue>
        <cue name="LogGuide_TradeRange_Cluster" instantiate="true" namespace="this">
            <conditions>
                <event_cue_signalled/>
            </conditions>
            <actions>
                <set_value name="$idcode" exact="event.param.$object.idcode" />
                <set_value name="global.$v1024_logGuideTradeRangeTable.$idcode" exact="'cluster'" />
                <show_help position="1" duration="6s" custom="'V1024 Logistics Guidance: Trade range becomes cluster.'" chance="0"/>
                <speak actor="player.computer" priority="-30">
                    <!--
                        In case for Betty, you can tell her to speak other supported lines by specifying the page number.
                    -->
                    <text line="134" comment="New command set." />
                    <text line="145" comment="Control established." />
                </speak>
            </actions>
        </cue>
        <cue name="LogGuide_TradeRange_Reset" instantiate="true" namespace="this">
            <conditions>
                <event_cue_signalled/>
            </conditions>
            <actions>
                <set_value name="$idcode" exact="event.param.$object.idcode" />
                <remove_value name="global.$v1024_logGuideTradeRangeTable.$idcode" />
                <show_help position="1" duration="6s" custom="'V1024 Logistics Guidance: Trade range becomes vanilla.'" chance="0"/>
                <speak actor="player.computer" priority="-30">
                    <!--
                        In case for Betty, you can tell her to speak other supported lines by specifying the page number.
                    -->
                    <text line="135" comment="Cancelled." />
                </speak>
            </actions>
        </cue>
        
        <!-- Mine range -->
        <cue name="LogGuide_MineRange_Sector" instantiate="true" namespace="this">
            <conditions>
                <event_cue_signalled/>
            </conditions>
            <actions>
                <set_value name="$idcode" exact="event.param.$object.idcode" />
                <set_value name="global.$v1024_logGuideMineRangeTable.$idcode" exact="'sector'" />
                <show_help position="1" duration="6s" custom="'V1024 Logistics Guidance: Mine range becomes sector.'" chance="0"/>
                <speak actor="player.computer" priority="-30">
                    <!--
                        In case for Betty, you can tell her to speak other supported lines by specifying the page number.
                    -->
                    <text line="134" comment="New command set." />
                    <text line="145" comment="Control established." />
                </speak>
            </actions>
        </cue>
        <cue name="LogGuide_MineRange_Cluster" instantiate="true" namespace="this">
            <conditions>
                <event_cue_signalled/>
            </conditions>
            <actions>
                <set_value name="$idcode" exact="event.param.$object.idcode" />
                <set_value name="global.$v1024_logGuideMineRangeTable.$idcode" exact="'cluster'" />
                <show_help position="1" duration="6s" custom="'V1024 Logistics Guidance: Mine range becomes cluster.'" chance="0"/>
                <speak actor="player.computer" priority="-30">
                    <!--
                        In case for Betty, you can tell her to speak other supported lines by specifying the page number.
                    -->
                    <text line="134" comment="New command set." />
                    <text line="145" comment="Control established." />
                </speak>
            </actions>
        </cue>
        <cue name="LogGuide_MineRange_Reset" instantiate="true" namespace="this">
            <conditions>
                <event_cue_signalled/>
            </conditions>
            <actions>
                <set_value name="$idcode" exact="event.param.$object.idcode" />
                <remove_value name="global.$v1024_logGuideMineRangeTable.$idcode" />
                <show_help position="1" duration="6s" custom="'V1024 Logistics Guidance: Mine range becomes vanilla.'" chance="0"/>
                <speak actor="player.computer" priority="-30">
                    <!--
                        In case for Betty, you can tell her to speak other supported lines by specifying the page number.
                    -->
                    <text line="135" comment="Cancelled." />
                </speak>
            </actions>
        </cue>
        
        <!-- Salvage range -->
        <cue name="LogGuide_SalvageRange0" instantiate="true" namespace="this">
            <conditions>
                <event_cue_signalled/>
            </conditions>
            <actions>
                <set_value name="$idcode" exact="event.param.$object.idcode" />
                <set_value name="global.$v1024_logGuideSalvageRangeTable.$idcode" exact="0" />
                <show_help position="1" duration="6s" custom="'V1024 Logistics Guidance: Salvage range becomes 0.'" chance="0"/>
                <speak actor="player.computer" priority="-30">
                    <!-- 
                        In case for Betty, you can tell her to speak other supported lines by specifying the page number.
                    -->
                    <text line="145" comment="Control established." />
                </speak>
            </actions>
        </cue>
        <cue name="LogGuide_SalvageRange999" instantiate="true" namespace="this">
            <conditions>
                <event_cue_signalled/>
            </conditions>
            <actions>
                <set_value name="$idcode" exact="event.param.$object.idcode" />
                <remove_value name="global.$v1024_logGuideSalvageRangeTable.$idcode" />
                <show_help position="1" duration="6s" custom="'V1024 Logistics Guidance: Salvage range becomes vanilla.'" chance="0"/>
                <speak actor="player.computer" priority="-30">
                    <!-- 
                        In case for Betty, you can tell her to speak other supported lines by specifying the page number.
                    -->
                    <text line="134" comment="New command set." />
                </speak>
            </actions>
        </cue>
    </cues>
</mdscript>
